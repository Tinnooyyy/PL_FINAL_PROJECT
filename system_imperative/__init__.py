"""
System B: the Imperative System (imperative backend + its own server + its own front end).

This file only lets the top-level tests import `system_imperative.backend`.
The system itself never imports anything from outside this folder.
"""
