"""
System A: the OOP System (object-oriented backend + its own server + its own front end).

This file only lets the top-level tests import `system_oop.backend`.
The system itself never imports anything from outside this folder.
"""
